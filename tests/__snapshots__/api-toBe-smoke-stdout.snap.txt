···· TSTyche <<version>> at <<basePath>>/tests/__fixtures__/api-toBe

uses TypeScript <<version>> with ./tsconfig.json

pass ./__typetests__/smoke.tst.ts
  + is any?
  + is NOT any?
  + is unknown?
  + is NOT unknown?
  + is string?
  + is NOT string?
  + is number?
  + is NOT number?
  + is bigint?
  + is NOT bigint?
  + is boolean?
  + is NOT boolean?
  + is symbol?
  + is NOT symbol?
  + is object?
  + is NOT object?
  + is void?
  + is NOT void?
  + is undefined?
  + is NOT undefined?
  + is null?
  + is NOT null?
  + is never?
  + is NOT never?
  + is 'abc'?
  + is NOT 'abc'?
  + is 'def'?
  + is NOT 'def'?
  + is 123?
  + is NOT 123?
  + is 456?
  + is NOT 456?
  + is 887n?
  + is NOT 887n?
  + is 998n?
  + is NOT 998n?
  + is true?
  + is NOT true?
  + is false?
  + is NOT false?
  + is string | number?
  + is NOT string | number?
  + is string | Array<string>?
  + is NOT string | Array<string>?
  + is string | Array<{ a: string }>?
  + is NOT string | Array<{ a: string }>?
  + is { a: string } | { b: string }?
  + is NOT { a: string } | { b: string }?
  + is { a: string } | { b: number }?
  + is NOT { a: string } | { b: number }?
  + is { a: string } & { b: string }?
  + is NOT { a: string } & { b: string }?
  + is { a: string } & { b: number }?
  + is NOT { a: string } & { b: number }?
  + is { a: string }?
  + is NOT { a: string }?
  + is { a?: string }?
  + is NOT { a?: string }?
  + is { readonly a: string }?
  + is NOT { readonly a: string }?
  + is { readonly a?: string }?
  + is NOT { readonly a?: string }?
  + is Array<string>?
  + is NOT Array<string>?
  + is Array<Array<string>>?
  + is NOT Array<Array<string>>?
  + is number[]?
  + is NOT number[]?
  + is ReadonlyArray<string>?
  + is NOT ReadonlyArray<string>?
  + is ReadonlyArray<ReadonlyArray<string>>?
  + is NOT ReadonlyArray<ReadonlyArray<string>>?
  + is Array<{ a: string }>?
  + is NOT Array<{ a: string }>?
  + is { a: number }[]?
  + is NOT { a: number }[]?
  + is ReadonlyArray<{ a: string }>?
  + is NOT ReadonlyArray<{ a: string }>?
  + is { readonly a: Array<string> }?
  + is NOT { readonly a: Array<string> }?
  + is { readonly a: number[] }?
  + is NOT { readonly a: number[] }?
  + is { readonly a: ReadonlyArray<string> }?
  + is NOT { readonly a: ReadonlyArray<string> }?
  + is [string]?
  + is NOT [string]?
  + is [number]?
  + is NOT [number]?
  + is [string?]?
  + is NOT [string?]?
  + is readonly [string]?
  + is NOT readonly [string]?
  + is readonly [string?]?
  + is NOT readonly [string?]?
  + is Promise<number>?
  + is NOT Promise<number>?
  + is Promise<void>?
  + is NOT Promise<void>?
  + is Promise<{ a: string }>?
  + is NOT Promise<{ a: string }>?
  + is Promise<Array<{ a: string }>>?
  + is NOT Promise<Array<{ a: string }>>?
  + is Promise<ReadonlyArray<{ a: string }>>?
  + is NOT Promise<ReadonlyArray<{ a: string }>>?
  + is () => void?
  + is NOT () => void?
  + is <T>() => T?
  + is NOT <T>() => T?
  + is <T>(a: T) => T?
  + is NOT <T>(a: T) => T?
  + is <T>(this: void) => T?
  + is NOT <T>(this: void) => T?
  + is <const T>(a: T) => T?
  + is NOT <const T>(a: T) => T?
  + is <T>(a: NoInfer<T>) => T?
  + is NOT <T>(a: NoInfer<T>) => T?

Targets:    1 passed, 1 total
Test files: 1 passed, 1 total
Tests:      116 passed, 116 total
Assertions: 3364 passed, 3364 total
Duration:   <<timestamp>>
